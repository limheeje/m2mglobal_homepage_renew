---
export interface Props {
  size?: 'sm' | 'md' | 'lg'
  tleft?: boolean
}
const {size = 'md', tleft} = Astro.props
---

<table
  class="bs-table"
  class:list={{
    'is-tleft': tleft
  }}
  data-size={size}
>
  <slot />
</table>
